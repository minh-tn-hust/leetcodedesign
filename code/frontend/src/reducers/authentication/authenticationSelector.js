export const currentRole = (state) => state.authentication.role;

export const getSuccessMessage = (state) => state.authentication.successMessage;
export const getFailMessage = (state) => state.authentication.failMessage;
export const getAuthenRole = (state) => state.authentication.role;

export const getUsername = (state) => state.authentication.username;
